import express from 'express';
const router = express.Router();


router.get('/server/:serverID/filter', (req, res) => {
    
});

router.get('/server/:serverID/filter/:role', (req, res) => {
    
});

export default router;